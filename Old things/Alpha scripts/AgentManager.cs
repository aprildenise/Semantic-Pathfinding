using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AgentManager : MonoBehaviour {
    /*
    private HPAManager HPA;
    public Map m;
    private AStarAlgo astar;
    public ZoneManager zm;

    public Transform agent;
    public Transform goal;

    private Dictionary<string, int> gCost; //distance from start
    private Dictionary<string, int> hCost; //heuristics

    public Transform Agent()
    {
        return agent;
    }
    public Transform Goal()
    {
        return goal;
    }
    // Use this for initialization
    void Start()
    {
        Map map = new Map();
        map.InitMap();

        HPA.HPAFindPath(this.agent.position, this.goal.position);
    }

    // Update is called once per frame
    void Update()
    {

    }
    public void Replan(int rate)
    {
        //placeholder function to determin when for the agent replan its path

    }

    public int getGCost(string id)
    {
        return gCost[id];
    }

    public void setGCost(string id, int f)
    {
        gCost.Add(id, f);
    }

    public int getHCost(string id)
    {
        return hCost[id];
    }

    public void setHCost(string id, int h)
    {
        hCost.Add(id, h);
    }
    */
}

